<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.0/css/bootstrap.min.css">
    <link rel="stylesheet" href="css/style.css">
    <link href="https://fonts.googleapis.com/css?family=Open+Sans" rel="stylesheet">
    <script src="https://cdnjs.cloudflare.com/ajax/libs/axios/0.18.0/axios.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/vue@2.5.16/dist/vue.js"></script>
    <!-- <script>
        let app = new Vue({
    el: '#prod',
    data: {
        products: '',
        totalAmount: ''
    },
    created() {
        axios.get('/api/cart')
            .then((productList) => {
                this.products = productList.data
                console.log("cart has " + this.products.length + " products")
                let sum = 0;
                for (let i = 0; i < this.products.length; i++) {
                    sum += this.products[i].amount;
                }
                this.totalAmount = sum
                console.log("the total amount is : " + this.totalAmount)
            })
            .catch((err) => {
                console.log(err)
            })
    },
    methods: {
        addToCart(event) {
            productId=event.target.getAttribute('data-id');
            fetch('addItem',{method:'POST',credentials:'include', productId: parseInt(event.target.getAttribute('data-id'))})
            .then(res=>res.json())
            .then(jsonRes=>{
                document.inner
            })

            console.log('id------'+event.target.getAttribute('data-id'))
            axios.post('/api/cart', {
                productId: parseInt(event.target.getAttribute('data-id'))
            })
                .then((cart) => {
                    console.log("Incremented the product")
                    this.products = cart.data
                    let sum = 0;
                    for (let i = 0; i < this.products.length; i++) {
                        sum += this.products[i].amount;
                    }
                    this.totalAmount = sum
                }).catch((err) => {
                    console.log(err);
                })
        },
        removeFromCart() {
            console.log('id------'+event.target.getAttribute('data-id'))
            axios.put('dropitem', {
                productId: parseInt(event.target.getAttribute('data-id'))
            }).then((cart) => {
                console.log("decremented the product")
                this.products = cart.data
                let sum = 0;
                for (let i = 0; i < this.products.length; i++) {
                    sum += this.products[i].amount;
                }
                this.totalAmount = sum
            })
        }
    }
})
///////////////////////////////////////////////
addToCart=function(event){
    $.ajax({
        url:"additem",
        data="id="+event.target.getAttribute('data-id'),
        type:"POST",
        dataType:"json",
        success:function(items){
            var data1="<h1>Hello world</h1>";
            // var data1="<% console.log('client side',usertype,items.length,udata,items); 
            // if(usertype=='customer'){
            //    var total=0,sumTotal=0;";
            // for(var i=0;i<items.length;i++){
            //     data1+='<div>
            //                 <tr>
            //                 <td><%=items[i].name%></td>
            //                 <td><%=items[i].price%></td>
            //                 <td>
            //                     <button class="btn" data-id="<%items[i].id%>" onclick="removeFromCart(items[i])">
            //                         <b>-</b>
            //                     </button>
            //                     <%=items[i].quantity %>
            //                     <button class="btn" data-id="<%items[i].id%>" onclick="addToCart(items[i])">
            //                         <b>+</b>
            //                     </button>
            //                 </td>
            //                 <td><%= total %> </td>
            //             </tr> 
            //             <tr style="background-color: white; font-size: 24px">
            //                 <td style="color: black">
            //                     <b>Total</b>
            //                 </td>
            //                 <td></td>
            //                 <td></td>
            //                 <td style="color: black">
            //                     <b>Rs.<%= sumTotal%> /-</b>
            //                 </td>
            //             </tr> </div>';      
            // }

            // data1+="<%  
            //             }
            //         }  
            // %>";
        //("#cartbody").html(data1);
        }
    });
 //         console.log(items);
            //            var data1="<% console.log('client side',usertype,items.length,udata,items);"; 
            // data1+="if(usertype=='customer'){";
            //         data+="var total=0,sumTotal=0;";
            //         data1+="for(var i=0;i<items.length;i++){%>";
            //         data1+='<div><tr><td>';
            //         data1+='<%=items[i].name%>';
            //         data1+='</td><td>';
            //         data1+='<%=items[i].price%>';
            //         data1+='</td><td><button class="btn" data-id="<%items[i].id%>" onclick="removeFromCart(items[i])"><b>-</b></button><%=items[i].quantity %><button class="btn" data-id="<%items[i].id%>" onclick="addToCart(items[i])">
            //         data1+='<b>+</b></button></td><td><%= total %> </td></tr>';
            //         data1+='<tr style="background-color: white; font-size: 24px"><td style="color: black"><b>Total</b></td><td></td><td></td><td style="color: black"><b>Rs.<%= sumTotal%> /-</b></td></tr> </div>';      
            // data1+='} ';

            // data1+="<% }}  %>";
///////////////////////////////////////////////




}

removeCart=function(data){

}
    </script> -->
    <title>Cart</title>
</head>

<body>
    <header class="cl-header">
        <div>
            <img src="/images/logo.jpg" alt="logo" class="cl-logo"/>
            <h3 class="cl-company-name">
                <b>House Store</b>
            </h3>
            <nav class="cl-nav">
                <ul>
                    <li>
                        <a href="dashboard.html">Home</a>
                    </li>
                    <li>
                        <a href="addProduct.html">Add Product</a>
                    </li>
                    <li>
                        <a href="logoutme">Logout</a>
                    </li>
                </ul>
            </nav>
        </div>
    </header>


    <div id="prod">
        <center>
            <br/>
            <br/>
            <h2 class="heading">Product Cart</h2>
            <br>
            <table style="width: 800px" class="table">
                <thead>
                    <th>
                        <b>Name</b>
                    </th>
                    <th>
                        <b>Price</b>
                    </th>
                    <th>
                        <b>Quantity</b>
                    </th>
                    <th>
                        <b>Amount</b>
                    </th>
                </thead>
                <tbody>
            <!--       <%
             console.log('client side',usertype,items.length,udata,items); 
            if(usertype=='customer'){ 
               var total=0,sumTotal=0;
                        for(var i=0;i<items.length;i++){ %>
                       <div>
                            <tr>
                            <td><%=items[i].name %></td>
                            <td><%=items[i].price %></td>
                            <td>
                                <button class="btn"  onclick="removeFromCart()"> //data-id="<%=items[i].id%>"
                                    <b>-</b>
                                </button>
                                <%=items[i].quantity %>
                                <button class="btn" data-id="<%=items[i].id%>" onclick="addToCart()">
                                    <b>+</b>
                                </button>
                            </td>
                            <td><%=total %> </td>
                        </tr> 
                        <tr style="background-color: white; font-size: 24px">
                            <td style="color: black">
                                <b>Total</b>
                            </td>
                            <td></td>
                            <td></td>
                            <td style="color: black">
                                <b>Rs.<%=sumTotal%> </b>
                            </td>
                        </tr> </div>
                           <%  
                        }
                    }  
            %>                  
            %>-->
                </tbody>
            </table>
   

    </center>
    <!-- <script src="./shoppingCart.js"></script> -->
</body>

</html>